{
  "name": "tabhere",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "postbuild": "node scripts/copy-locales.mjs",
    "version:set": "node scripts/set-version.mjs",
    "package": "node scripts/package.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.256",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
